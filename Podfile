# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Audio_Recorder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Audio_Recorder
  	pod 'Firebase/Core'
    pod 'Firebase/Storage'
    pod 'FirebaseUI'
    pod 'FirebaseUI/Auth'
    pod 'Firebase/Auth'
    pod 'Firebase/Database'
    pod 'Pulsator'

  target 'Audio_RecorderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Audio_RecorderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
